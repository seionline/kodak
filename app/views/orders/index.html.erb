<h1><%= t(@orders.model_name.i18n_key, count: @orders.count) %></h1>

<p>
  <%# TODO i18n %>
  Note: Orders will be deleted automatically after they are fulfilled.
</p>

<% if @orders.any? %>

  <table>
    <tbody>
      <% @orders.each do |order| %>
        <tr>
          <td>
            <%= link_to edit_order_path(order) do %>
              <%# TODO i18n %>
              Order from <%= l(order.updated_at) %>
            <% end %>
          </td>
          <td><%= order.status %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

<% else %>
  <p>
    <%# TODO i18n %>
    You don't have any orders.
  </p>
<% end %>

<%= link_to "Create order", new_order_path %>